import UI from "../components/ui-components";

const Dashboard = () => {
  return (
    <UI.Section class_name={"authenticated-dashboard"}>
      <UI.Section class_name={"authenticated-dashboard"}>
        <h1>
          Dashboard Page | To be authorized Page via Cookie Authentication{" "}
        </h1>
      </UI.Section>
    </UI.Section>
  );
};

export default Dashboard;
