import { ReactNode } from "react";

interface Props {
    text?: String;
    id?: String;
    to?:String;
    htmlFor?:String;
    class_name?: String;
    linkText?:String;
    children?: ReactNode;
}

export default Props;